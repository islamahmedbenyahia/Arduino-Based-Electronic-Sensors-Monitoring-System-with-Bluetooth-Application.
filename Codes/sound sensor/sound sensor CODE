oolean statuslamp;

void setup() {
   Serial.begin(9600);
   pinMode(12, OUTPUT);
   pinMode(2, INPUT);
   statuslamp=false;
}

void loop() {
   Serial.println(digitalRead(2));
  
   if(digitalRead == HIGH) {
      statuslamp=!statuslamp;
      digitalWrite(12, statuslamp);
      delay(20);
   }
}
